<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PussyCatsApp.views.TestDashboardView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PussyCatsApp.views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loaded="Page_Loaded"
    Background="#7B4F8E">

    <Grid Padding="24" RowSpacing="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top bar -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="BACK"
                    Background="#9B6BAE" Foreground="White"
                    BorderThickness="0" CornerRadius="6"
                    Click="BackButton_Click"/>

            <TextBlock Grid.Column="1" Text="Skill Tests"
                       Foreground="White" FontSize="18" FontWeight="Medium"
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>

            <Button Grid.Column="2" Content="GO TO ALL TESTS"
                    Background="#9B6BAE" Foreground="White"
                    BorderThickness="0" CornerRadius="6"
                    Click="GoToAllTestsButton_Click"/>
        </Grid>

        <!-- Scrollable single-column card list -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="TestCardsContainer" Spacing="14"/>
        </ScrollViewer>
    </Grid>
</Page>

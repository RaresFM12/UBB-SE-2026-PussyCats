<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PussyCatsApp.Views.CompatibilityOverviewView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Loaded="Page_Loaded">

	<Grid Margin="24">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
			<TextBlock Text="Role Compatibility" FontSize="24" FontWeight="SemiBold" VerticalAlignment="Center"/>
            <Button Grid.Row="0" Content="Back" Click="ButtonBack_Click" Margin="15,15,0,16"/>
        </StackPanel>

		<ListView x:Name="rolesList" Grid.Row="1" SelectionChanged="RolesList_SelectionChanged">
			<ListView.ItemTemplate>
				<DataTemplate>
					<Grid Margin="0,8,0,8">
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="200"/>
							<ColumnDefinition Width="*"/>
							<ColumnDefinition Width="80"/>
						</Grid.ColumnDefinitions>
						<TextBlock Grid.Column="0" Text="{Binding DisplayName}" VerticalAlignment="Center"/>
						<ProgressBar Grid.Column="1" Minimum="0" Maximum="100" Value="{Binding DisplayScore}" Margin="12,0,12,0" VerticalAlignment="Center"/>
						<TextBlock Grid.Column="2" Text="{Binding DisplayPercentage}" VerticalAlignment="Center" HorizontalAlignment="Right"/>
					</Grid>
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>
	</Grid>
</Page>
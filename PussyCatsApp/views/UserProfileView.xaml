<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PussyCatsApp.views.UserProfileView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PussyCatsApp.views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer>
        <StackPanel>
            <local:ProfileCompletenessBar x:Name="completenessBar" />

            <Grid Padding="20,40,20,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Vertical" Spacing="15" Margin="40,0,40,0" VerticalAlignment="Center">
                    <PersonPicture x:Name="pbAvatar" Width="200" Height="200" HorizontalAlignment="Center" />

                    <StackPanel Orientation="Horizontal" Spacing="10" HorizontalAlignment="Center">
                        <Button x:Name="btnUploadAvatar" Content="Upload avatar" Click="OnAvatarUploadClick" Width="140" />
                        <Button x:Name="btnRemoveAvatar" Content="Remove avatar" Click="OnAvatarRemoveClick" Width="140" />
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Vertical" Spacing="20" VerticalAlignment="Center">
                    <Border CornerRadius="12" Padding="20" MinWidth="220" HorizontalAlignment="Left" BorderThickness="1" BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}">
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="12">
                            <TextBlock Text="Account Status" FontSize="14" FontWeight="Bold" HorizontalAlignment="Center" />
                            <ToggleSwitch x:Name="chkAccountStatus" Toggled="OnStatusToggle" OnContent="Active" OffContent="Inactive" HorizontalAlignment="Center" />
                        </StackPanel>
                    </Border>

                    <StackPanel Spacing="6" Margin="0,16,0,0" MaxWidth="320">
                        <TextBlock x:Name="LevelTitleText" FontSize="16" FontWeight="Medium" />
                        <ProgressBar x:Name="XpProgressBar" Minimum="0" Maximum="100" Height="12" />
                        <TextBlock x:Name="XpCountText" FontSize="12" Opacity="0.7" />
                    </StackPanel>
                </StackPanel>

                <TextBlock x:Name="lblFreshness" Grid.Column="1" Text="Profile last updated:..." HorizontalAlignment="Right" VerticalAlignment="Bottom" Opacity="0.7" FontSize="13" />
            </Grid>

            <Grid Padding="30,60,30,80" MinHeight="600">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Vertical" Spacing="25" HorizontalAlignment="Left" Margin="20,0,0,0">
                    <TextBlock x:Name="lblFirstName" Text="First Name: " FontSize="16" />
                    <TextBlock x:Name="lblLastName" Text="Last Name: " FontSize="16" />
                    <TextBlock x:Name="lblEmail" Text="Email: " FontSize="16" />
                    <TextBlock x:Name="lblPhone" Text="Phone: " FontSize="16" />
                    <TextBlock x:Name="lblGithubAccount" Text="GitHub: " FontSize="16" />
                    <TextBlock x:Name="lblLinkedinAccount" Text="LinkedIn: " FontSize="16" />
                    <TextBlock x:Name="lblGender" Text="Gender: " FontSize="16" />
                    <TextBlock x:Name="lblUniversity" Text="University: " FontSize="16" />
                    <TextBlock x:Name="lblCountry" Text="Country: " FontSize="16" />
                    <TextBlock x:Name="lblAddress" Text="Address: " FontSize="16" />
                    <TextBlock x:Name="lblGraduationYear" Text="Graduation Year: " FontSize="16" />
                    <TextBlock x:Name="lblError" Foreground="Red" TextWrapping="Wrap" Margin="0,20,0,0" />
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Vertical" Spacing="25" HorizontalAlignment="Right" Width="300" Margin="0,0,75,0">
                    <Button x:Name="btnEdit" Content="Edit Profile" HorizontalAlignment="Stretch" />
                    <Button x:Name="btnPersonalityTest" Content="Take Personality Test" HorizontalAlignment="Stretch" />

                    <Button x:Name="btnPreviewCV" Content="Preview &amp; Export CV" Click="OnPreviewCVClick" HorizontalAlignment="Stretch" />

                    <Button x:Name="btnViewDocuments" Content="View Documents" HorizontalAlignment="Stretch" />
                    <Button x:Name="btnMatchHistory" Content="Match History" HorizontalAlignment="Stretch" />
                    <Button x:Name="btnRoleCompatibility" Content="Role Compatibility Analyzer" Click="OnCompatibilityAnalyzerClick"  HorizontalAlignment="Stretch" />
                    <Button x:Name="btnPublicProfile" Content="See Public Profile" HorizontalAlignment="Stretch" />
                    <Button x:Name="btnOldTests" Content="Go to old tests" HorizontalAlignment="Stretch" />
                    <Button x:Name="btnSkillTests" Content="Skill Tests" HorizontalAlignment="Stretch" />
                </StackPanel>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</Page>
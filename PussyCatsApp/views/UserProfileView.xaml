<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PussyCatsApp.views.UserProfileView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PussyCatsApp.views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer>
        <StackPanel>
            <Grid Background="#c099cc" Padding="20,40,20,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Vertical" Spacing="15" Margin="40,0,40,0" VerticalAlignment="Center">
                    <PersonPicture x:Name="pbAvatar" Width="200" Height="200" HorizontalAlignment="Center" />
                    <StackPanel Orientation="Horizontal" Spacing="10" HorizontalAlignment="Center">
                        <Button x:Name="btnUploadAvatar" Content="Upload avatar" Click="OnAvatarUploadClick" Width="140" Background="#724780" Foreground="White" />
                        <Button x:Name="btnRemoveAvatar" Content="Remove avatar" Click="OnAvatarRemoveClick" Width="140" Background="#724780" Foreground="White" />
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="20" VerticalAlignment="Center">
                    <Border Background="#9b72a8" CornerRadius="12" Padding="20" MinWidth="180">
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                            <TextBlock Text="Account Status" Foreground="White" FontSize="14" FontWeight="Bold" Margin="0,0,0,10" HorizontalAlignment="Center"/>
                            <ToggleSwitch x:Name="chkAccountStatus" Toggled="OnStatusToggle" OnContent="Active" OffContent="Inactive" Foreground="White" HorizontalAlignment="Center" />
                        </StackPanel>
                    </Border>
                </StackPanel>

                <TextBlock x:Name="lblFreshness" Grid.Column="1" Text="Profile last updated:..." HorizontalAlignment="Right" VerticalAlignment="Bottom" Foreground="Black" Opacity="0.7" FontSize="13" />
            </Grid>

            <Grid Background="#724780" Padding="30,60,30,80" MinHeight="600">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Vertical" Spacing="25" HorizontalAlignment="Left" Margin="20,0,0,0">
                    <TextBlock x:Name="lblFirstName" Text="First Name: " Foreground="White" FontSize="16" />
                    <TextBlock x:Name="lblLastName" Text="Last Name: " Foreground="White" FontSize="16" />
                    <TextBlock x:Name="lblEmail" Text="Email: " Foreground="White" FontSize="16" />
                    <TextBlock x:Name="lblPhone" Text="Phone: " Foreground="White" FontSize="16" />
                    <TextBlock x:Name="lblGithubAccount" Text="GitHub: " Foreground="White" FontSize="16" />
                    <TextBlock x:Name="lblLinkedinAccount" Text="LinkedIn: " Foreground="White" FontSize="16" />
                    <TextBlock x:Name="lblGender" Text="Gender: " Foreground="White" FontSize="16" />
                    <TextBlock x:Name="lblCountry" Text="Country: " Foreground="White" FontSize="16" />
                    <TextBlock x:Name="lblCity" Text="City: " Foreground="White" FontSize="16" />
                    <TextBlock x:Name="lblGraduationYear" Text="Graduation Year: " Foreground="White" FontSize="16" />
                    <TextBlock x:Name="lblError" Foreground="#FFA0A0" TextWrapping="Wrap" Margin="0,20,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Vertical" Spacing="25" HorizontalAlignment="Right" Width="300" Margin="0,0,75,0">
                    <Button x:Name="btnEdit" Content="Edit Profile" HorizontalAlignment="Stretch" Background="#c099cc" Foreground="Black"/>
                    <Button x:Name="btnPersonalityTest" Content="Take Personality Test" HorizontalAlignment="Stretch" Background="#c099cc" Foreground="Black"/>
                    <Button x:Name="btnExportPdf" Content="Export to PDF" HorizontalAlignment="Stretch" Background="#c099cc" Foreground="Black"/>
                    <Button x:Name="btnViewDocuments" Content="View Documents" HorizontalAlignment="Stretch" Background="#c099cc" Foreground="Black"/>
                    <Button x:Name="btnMatchHistory" Content="Match History" HorizontalAlignment="Stretch" Background="#c099cc" Foreground="Black"/>
                    <Button x:Name="btnPublicProfile" Content="See Public Profile" HorizontalAlignment="Stretch" Background="#c099cc" Foreground="Black"/>
                    <Button x:Name="btnOldTests" Content="Go to old tests" HorizontalAlignment="Stretch" Background="#c099cc" Foreground="Black"/>
                    <Button x:Name="btnSkillTests" Content="Skill Tests" HorizontalAlignment="Stretch" Background="#c099cc" Foreground="Black"/>
                </StackPanel>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</Page>
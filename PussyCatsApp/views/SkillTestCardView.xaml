<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="PussyCatsApp.views.SkillTestCardView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PussyCatsApp.views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">


    <Border Background="#6A3F80" CornerRadius="12" Padding="18,14">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Badge icon -->
            <Image x:Name="BadgeIcon" Grid.Column="0"
                   Width="44" Height="44" VerticalAlignment="Center"/>

            <!-- Test info -->
            <StackPanel Grid.Column="1" Margin="16,0" Spacing="4" VerticalAlignment="Center">
                <TextBlock x:Name="TestNameText"
                           Foreground="White" FontSize="14" FontWeight="Medium"/>
                <TextBlock x:Name="ScoreText"
                           Foreground="#D4B8E0" FontSize="12"/>
                <TextBlock x:Name="DateText"
                           Foreground="#D4B8E0" FontSize="12"/>
            </StackPanel>

            <!-- Retake button -->
            <Button x:Name="RetakeButton" Grid.Column="2"
                    Content="RETAKE"
                    Background="#9B6BAE" Foreground="White"
                    BorderThickness="0" CornerRadius="6"
                    VerticalAlignment="Center"
                    Click="RetakeButton_Click"/>
        </Grid>
    </Border>
</UserControl>

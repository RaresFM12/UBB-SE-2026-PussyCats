<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="PussyCatsApp.views.DocumentsPage" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Documents" Style="{StaticResource HeaderTextBlockStyle}" HorizontalAlignment="Center" Margin="0,0,0,20"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="10" Margin="0,0,0,20">
            <Button x:Name="UploadDocumentButton" Click="UploadDocumentButton_Click">
                <StackPanel Orientation="Horizontal" Spacing="5">
                    <FontIcon Glyph="&#xE898;"/>
                    <TextBlock Text="Upload Document"/>
                </StackPanel>
            </Button>
            <InfoBar x:Name="DocumentInfoBar" Severity="Informational" IsOpen="False"/>
        </StackPanel>
        <ListView Grid.Row="2" x:Name="DocumentsListView" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="10" ColumnSpacing="15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <FontIcon Grid.Column="0" Glyph="&#xE8A5;" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" Text="{Binding NameDocument}" VerticalAlignment="Center" FontSize="14" TextTrimming="CharacterEllipsis"/>
                        <Button Grid.Column="2" Content="Rename" VerticalAlignment="Center" Tag="{Binding}" Click="RenameButton_Click"/>
                        <Button Grid.Column="3" Content="Remove" VerticalAlignment="Center" Tag="{Binding}" Click="RemoveButton_Click"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <TextBlock Grid.Row="2" x:Name="EmptyText" Text="No documents yet." HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Visibility="Visible"/>
    </Grid>
</Page>